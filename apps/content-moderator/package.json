{
  "name": "genlayer-content-moderator",
  "version": "0.3.0",
  "description": "AI content moderation Intelligent Contract on GenLayer with on-chain appeals: an LLM judges content against community rules, validators reach consensus on APPROVE/FLAG/REMOVE, and verdicts can be contested.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "deploy": "node --env-file=.env deploy.mjs",
    "interact": "node --env-file=.env interact.mjs",
    "test": "node --env-file=.env test.mjs"
  },
  "dependencies": {
    "genlayer-js": "^1.1.8",
    "viem": "^2.55.18"
  }
}
